export default function SignupPage() {
  return (
    <div className="mx-auto max-w-sm pt-10">
      <h1 className="font-heading text-2xl font-bold">Start your restaurant</h1>
      <p className="mt-2 text-sm text-plum-ink/60">
        Self-serve restaurant onboarding (create restaurant + first admin login)
        lands at the end of Phase 1. For now, restaurants are provisioned via the
        seed script / super-admin.
      </p>
    </div>
  );
}
